---
name: Input and Output Text
description: Takes text input, displays it in the text node editor, and returns it as output.
---

## Overview
This skill accepts text input, displays it in the text node editor UI, and returns the same text as output.

## What this skill does
This skill defines an App Action called `input-output-text` that takes an `input-text` string, sets it in the textarea, and returns it as `{ "output-text": string }`.
