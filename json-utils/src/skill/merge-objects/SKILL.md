---
name: merge-objects
description: Merge two JSON objects into one.
---

# Merge Objects

Takes two JSON objects and merges their properties into a single object. If both objects share a key, the value from the second object takes precedence.
