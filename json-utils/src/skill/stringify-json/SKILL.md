---
name: stringify-json
description: Convert a JSON object into a formatted JSON string.
---

# Stringify JSON

Takes a JSON object and converts it into a pretty-printed JSON string with 2-space indentation.
