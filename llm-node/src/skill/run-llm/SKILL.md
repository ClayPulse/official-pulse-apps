---
name: run-llm
description: Run a prompt through a selected LLM and return the response
---

# Run LLM

Use this skill to send a prompt to an AI language model. The model is selected via the UI (gpt-5.4 or gpt-5-mini). Returns the final text response from the model.
